# 오행핏(Ohang Fit) — Claude Code용 Phase별 개발 프롬프트

PRD 기준 Phase 1~3 개발 프롬프트. Claude Code 실행 시 프로젝트 루트에 PRD(ohang_fit_prd.md)를 같이 두고 시작하는 걸 권장함 (프롬프트에서 참조하게 되어 있음).

---

## Phase 1 (MVP) 프롬프트

```
너는 "오행핏(Ohang Fit)" 프로젝트의 풀스택 개발을 맡은 엔지니어야.
프로젝트 루트의 ohang_fit_prd.md를 먼저 읽고 전체 맥락(문제정의, 페르소나, 메시징 방향)을 파악한 뒤 아래 Phase 1(MVP) 범위로 개발을 시작해줘.

[기술 스택 / 프로젝트 구조]
- Frontend: Next.js (App Router), 모바일 우선 반응형 웹 (PC도 반응형으로 대응)
- Backend: Python FastAPI
- Database: PostgreSQL
- 구조: 모노레포 하나로 /frontend, /backend 디렉토리를 나눠서 작업 (레포 분리 X)
- 로컬 개발 환경은 docker-compose로 구성 (PostgreSQL은 필수로 컨테이너화, backend도 같이 컨테이너화해서 `docker compose up`으로 바로 백엔드+DB가 뜨게 만들어줘. frontend는 로컬에서 `npm run dev`로 돌리고 백엔드 컨테이너의 API를 바라보게 구성)

[Phase 1 범위]
1. 온보딩
   - 생년월일, 출생시간(모름 선택 가능), 성별, 태어난 지역 입력 폼
   - 잘못된 날짜/미래 날짜/1900년 이전 등 입력값 검증
   - 최초 입력 후 저장, 마이페이지에서 재입력/수정 가능
2. 분석 (오행)
   - 생년월일시를 사주 8자로 변환 후 오행(목/화/토/금/수) 분포 계산, 부족/과다 오행 도출
   - 사주 계산 로직은 외부 API 연동을 전제로 인터페이스(어댑터 패턴)를 분리해서 설계하고, 지금은 벤더가 정해지지 않았으니 동일 인터페이스의 자체 계산 로직(또는 목업)으로 우선 구현해줘. 나중에 실제 API로 교체하기 쉽게 만드는 게 핵심이야.
   - 신살(예: 화개살 등) 값도 함께 저장할 수 있도록 데이터 모델은 미리 확장 가능하게 설계 (Phase 1에서 신살 기반 소재 추천 로직까지 구현할 필요는 없음)
3. 큐레이션 (컬러, 자체 DB)
   - 부족한 오행 → 보완 컬러 매핑 테이블
   - 컬러와 매칭되는 아이템을 보여줄 수 있는 자체 아이템 DB(에디터가 넣는 형태) 스키마 및 관리용 CRUD(간단한 어드민 or 시딩 스크립트 수준으로 충분)
4. 무료 미리보기 + 결제(일회성)
   - 분석 완료 후 "부족한 오행 + 한 줄 요약"까지는 무료로 노출, 상세 컬러/아이템 결과는 잠금 처리
   - 결제 완료 시 잠금 해제되는 구조로 설계하되, 실제 PG 연동은 아직 벤더 미정이니 결제 성공/실패를 흉내내는 목업 결제 모듈로 구현해줘 (나중에 실제 PG로 교체하기 쉽게 인터페이스 분리)
5. 공유 카드 생성
   - "오늘의 컬러 + 추천 아이템" 요약한 이미지 카드 자동 생성 (인스타 스토리 9:16, 피드 1:1 비율 대응)
   - 워터마크/서비스명 + 신규 유입 링크 포함

[UX/카피 원칙]
- "패션 추천"이 아니라 "추천 아이템"이라는 표현 사용
- "오늘 뭐 입지" 대신 "오늘 뭘 입으면 나에게 도움이 될까" 톤
- UI 라벨은 "오늘의 컬러", "오늘의 추천 아이템"처럼 데일리 콘텐츠 느낌으로 (실제 값은 결제 시점 기준 고정값)

[비기능 요구사항]
- 모바일 UI 완성도 우선, PC는 반응형으로만 대응
- API 응답 1초 이내 목표
- 생년월일시 등 민감정보는 암호화 저장

[개발 순서]
1. 프로젝트 구조(폴더 구성), 전체 DB 스키마, API 엔드포인트 설계안을 먼저 제안하고 내 확인을 받는다
2. 온보딩 → 분석 → 큐레이션 → 결제 → 공유 순서로, 기능 하나씩 백엔드(DB+API)부터 구현한다
3. 해당 기능의 백엔드가 완성되면 바로 이어서 대응하는 프론트 화면을 구현하고 실제 API와 연동해서 테스트까지 마친다
4. 한 기능이 백엔드 → 프론트 → 연동 테스트까지 끝난 뒤에만 다음 기능으로 넘어간다 (프론트를 한번에 몰아서 만들지 말 것)
```

---

## Phase 2 프롬프트

```
"오행핏(Ohang Fit)" 프로젝트의 Phase 2 개발을 진행해줘. Phase 1(MVP)은 이미 구현되어 있고, 아래 범위를 이어서 작업하면 돼. 작업 전에 ohang_fit_prd.md와 기존 Phase 1 코드베이스 구조를 먼저 파악해줘.

[Phase 2 범위]
1. 소재/재질/핏 큐레이션 확장
   - 신살(예: 화개살 등) → 소재/재질 궁합 매핑 테이블 추가 (예: 화개살 보유자에게 가죽류 비추천)
   - 사용자 체형/취향 입력을 받아 핏 추천에 반영할 수 있도록 추가 설문/선택 입력 UI 및 데이터 모델 확장
   - 기존 컬러 큐레이션 로직과 자연스럽게 통합 (컬러+소재+핏을 함께 보여주는 결과 화면)
2. 재구매(win-back) 플로우
   - 큐레이션 로직/아이템 DB가 업데이트되면 기존 결제 사용자에게 알림(이메일/푸시 중 결정 필요, 우선 이메일로 구현) 발송
   - 할인된 가격으로 재분석을 신청/결제할 수 있는 플로우 추가
3. 개인화 고도화
   - Phase 1의 자체 아이템 DB를 확장하고, 추천 로직에 사용자별 과거 선택/선호를 반영할 수 있는 여지를 만들어줘 (구체 알고리즘은 제안해줘)

기존 Phase 1의 어댑터 패턴(사주 분석, 결제 모듈)을 유지하면서 확장하는 방식으로 설계해줘. 구현 전에 변경될 DB 스키마와 API 변경사항을 먼저 요약해서 보여줘.
Phase 1과 동일하게 소재/핏 큐레이션 → win-back 플로우 → 개인화 순으로, 기능마다 백엔드부터 구현하고 바로 프론트 연동 테스트까지 마친 뒤 다음 기능으로 넘어가는 방식으로 진행해줘.
```

---

## Phase 3 프롬프트

```
"오행핏(Ohang Fit)" 프로젝트의 Phase 3 개발을 진행해줘. Phase 1~2는 구현되어 있는 상태야. ohang_fit_prd.md와 기존 코드베이스를 먼저 확인하고 아래 범위로 작업해줘.

[Phase 3 범위]
1. 커머스 연동
   - 큐레이션된 아이템에 실제 구매 링크(제휴 커머스 API 또는 딥링크) 연결
   - 클릭/전환 트래킹 이벤트 추가 (KPI: 아이템 클릭 → 구매 전환율 측정 가능하도록)
2. 제휴 수수료 모델 대응
   - 제휴 커머스사별 트래킹 파라미터/딥링크 구조를 관리할 수 있는 구조 설계
3. 이 시점까지 쌓인 결제/전환 데이터를 바탕으로 대시보드(내부용, 간단한 관리자 화면 수준)로 KPI(온보딩 완료율, 결제 전환율, 공유율, win-back 전환율)를 확인할 수 있게 해줘

실제 제휴 커머스 벤더는 아직 미정이니, 인터페이스는 벤더 교체가 쉬운 구조로 설계하고 우선은 목업 데이터로 동작하게 구현해줘. 구현 전에 설계안을 먼저 요약해줘.
이전 Phase와 동일하게 기능 단위(커머스 연동 → 트래킹 → 대시보드)로 백엔드 구현 후 바로 프론트 연동 테스트하는 순서로 진행해줘.
```

---

### 참고

- 사주/오행 API 벤더, PG사, 제휴 커머스는 PRD상 아직 리서치 필요 상태라 프롬프트에서 목업/어댑터 패턴으로 우선 구현하도록 지시해뒀음. 벤더 확정되면 해당 부분만 교체하면 됨.
- 환불 정책은 아직 미정이라 Phase 1 프롬프트에는 포함 안 함 — 정하면 결제 모듈에 반영해서 추가하면 됨.
